/// Sorts car search results for the listing page.
class ResultSorter {
  /// TASK: defined by the Linear ticket — and by the spec YOU will
  /// approve during the exercise (questions first, then contract,
  /// then referee, then code).
  static List<Map<String, Object?>> byPriceAscending(
    List<Map<String, Object?>> results,
  ) {
    throw UnimplementedError('TODO: implement ResultSorter.byPriceAscending');
  }
}
